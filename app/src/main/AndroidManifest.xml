<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mypokemonapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPokemonApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.admin.DetailShortActivity"
            android:exported="true" />
        <activity
            android:name=".ui.admin.ReportActivity"
            android:exported="true" />
        <activity
            android:name=".ui.getfeedback.GetFeedBackActivity"
            android:exported="true" />
        <activity
            android:name=".ui.sendreport.SendReportActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.AddEditUserActivity"
            android:exported="true" />
        <activity
            android:name=".ui.admin.FeedBackActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.AdminActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".ui.activities.RegisterAccountActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".ui.activities.LoginActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.DetailActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:configChanges="screenSize|orientation|screenLayout|navigation" />
    </application>

</manifest>